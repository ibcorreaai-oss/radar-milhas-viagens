/node_modules
/.next/
/out/
.env
.env.local
.env*.local
*.tsbuildinfo
npm-debug.log*
.DS_Store
next-env.d.ts

# Dumps de backup do banco (supabase db dump) — contêm dados reais de
# usuário, nunca versionar. Ver DISASTER_RECOVERY.md.
/backups/
*.dump.sql
